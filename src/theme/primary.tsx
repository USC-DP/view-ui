import { createTheme } from "@mui/material";

const viewTheme = createTheme({
    palette: {
        primary: {
            main: '#F29047',
            contrastText: '#FFF',
            dark: '#db8444'

        }
    }
})

export default viewTheme;